.alert.alert-dismissible{:role => "alert", :class => "alert-#{type} alert"}
  %button.close{"aria-label" => "Close", :type => "button", data:{dismiss: 'alert'}}
    %span{"aria-hidden" => "true"}
      =image_tag 'close.png'
  - unless content.nil?
    #{content.is_a?(String) ? content.html_safe : content.join(', ').html_safe}

// types: info, danger, warning, success