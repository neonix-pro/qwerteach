%section.section
  .card
    .card-header.bordered
      .header-block
        %h4.title Top teachers
    .card-block
      .table-responsive
        %table.table.table-striped.table-hover.table-min
          %thead
            %tr
              %th{width: 30} Avatar
              %th Name
              %th Lessons given
              %th Amount
              %th{width: 50}
          %tbody
            - @top_teachers.each do |teacher|
              %tr
                %td= image_tag avatar_url(teacher), class: 'cell-avatar'
                %td= teacher.name
                %td= teacher.lessons_count
                %td= number_to_currency(teacher.lessons_amount)
                %td= link_to 'Show', admin_teacher_path(teacher), class: 'btn btn-sm btn-primary-outline'