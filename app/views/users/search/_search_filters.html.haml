%ul.list-inline
  %li.filter{data:{filter: nil}, class:('selected' if params[:filter].nil? || params[:filter].empty?)}
    %i.fa.fa-long-arrow-right
    Tous les profs
  %li.filter{data:{filter: :online}, class:('selected' if params[:filter] == 'online')}
    %i.fa.fa-check
    En ligne
  %li.filter{data:{filter: :first_lesson_free}, class:('selected' if params[:filter] == 'first_lesson_free')}
    %i.fa.fa-gift
    Premier cours gratuit
  %li.filter{data:{filter: :has_reviews}, class:('selected' if params[:filter] == 'has_reviews')}
    %i.fa.fa-star-o
    Appréciations