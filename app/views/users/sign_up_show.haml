.container-fluid
  .row
    %h2.page-title-green
      Inscrivez-vous
      pour en savoir plus sur #{@teacher.name}
  .row
    .col-sm-1
    .col-sm-5
      .additional-infos
        #step1-infos.booking-infos.active
          .teacher-recap
            = render :partial => 'lesson_requests/teacher_card', locals: {teacher: @teacher}
            = render :partial => 'lesson_requests/teacher_topics', locals: {teacher: @teacher}
            = render partial: 'lesson_requests/lesson_recap', locals: {lesson_request: @lesson_request}
            = render :partial => 'lesson_requests/teacher_references', locals:{teacher: @teacher} unless @teacher.reviews_received.empty?
            = render :partial => 'lesson_requests/teacher_reactivity', locals:{teacher: @teacher}
          .additional-links
            Pour plus d'informations à propos de:
            = link_to 'Garantie remboursement', faq_path, class: 'text-green', target: '_blank'
            = link_to 'Protection des données', faq_path, class: 'text-green', target: '_blank'
            = link_to 'Modes de payement sécurisés', faq_path, class: 'text-green', target: '_blank'
    .col-sm-5
      .sign-up-booking
        .sign-up-social
          %p.lead Inscription via
          .text-center
            = render "users/shared/omniauth_signin"
            %hr
            .or ou
        .signup-email
          %p.lead Inscription par e-mail
          .material-form= render 'devise/registrations/student_form'
        .text-right
          = link_to 'Déjà inscrit?', new_user_session_path


