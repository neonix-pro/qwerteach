.profSimilaire
  = link_to teacher.name, user_path(teacher) unless teacher.nil?
  = image_tag(teacher.avatar.url(:small), size: "100x100")
  %div
    - teacher.offers.each do |ad|
      = ad.topic.title
