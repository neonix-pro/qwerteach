%h3 Informations de payement
= form_for @account, as: :account, url:  create_account_user_lesson_requests_path(@teacher), remote: true do |f|
  = render 'mangopay/account_fields', f: f
  .field
    = submit_tag 'continuer', :class => 'btn btn-qw btn-green btn-mini'
    %br/

:javascript
  $('#new_account').on('submit', function(e){
    $('#lesson-details #step3 .lesson-payment-wrapper').addClass('hidden');
    $('#lesson-details .lesson-payment-loader').show();
  });