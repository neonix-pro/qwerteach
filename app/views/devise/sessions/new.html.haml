.container-fluid.sign_in_page
  .row
    %h1.text-center Se connecter
    .col-md-6.col-md-offset-3
      .omniauth-signup
        = render partial: 'users/shared/omniauth_signin'
      = form_for(resource, as: resource_name, url: session_path(resource_name), html:{class:'form form-horizontal'}) do |f|
        .form-group
          .col-sm-3
            = f.label :email
          .col-sm-9
            = f.email_field :email, autofocus: true , class: 'form-control', required: true, placeholder: 'jeannine@michel.com'
        .form-group
          .col-sm-3
            = f.label :password
          .col-sm-9
            = f.password_field :password, autocomplete: "off" , class: 'form-control', required: true
        - if devise_mapping.rememberable?
          .form-group
            .col-md-12
              = f.check_box :remember_me, checked: true
              = f.label :remember_me
        .actions.text-center
          = f.submit t('.sign_in')
  .row
    .col-md-6.col-md-offset-3.text-right
      .devise-links
        = link_to 'Pas encore inscrit?', new_user_registration_path
        %br
        =render partial: 'devise/shared/links'

