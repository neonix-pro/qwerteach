$('.show-lesson-details').hide();
$('<%= "#lesson-details-#{@lesson.id}" %>').slideDown('slow');
$('<%= "#lesson-details-#{@lesson.id}" %>').html('<%= j render partial: 'lessons/index/edit', locals:{lesson: @lesson} %>');