Recommanderiez-vous #{lesson.teacher.firstname} ?
%div
  = render partial: 'shared/star_rating', locals:{note: lesson.teacher.avg_reviews}
.todo_review.text-center.todo_action
  = button_to 'Laisser un avis', new_user_review_path(lesson.teacher.id), remote: true, method: :get, class: 'btn btn-qw btn-green btn-sm'