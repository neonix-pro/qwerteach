- if packs.empty?
  %h3 No lesson packs
- else
  %table.table.table-striped
    %thead
      %tr
        %th Topic
        %th Level
        %th # lessons
        %th amount
        %th
    %tbody
      - packs.each do |pack|
        %tr
          %td= pack.topic.try(:title)
          %td= pack.level.try(:title)
          %td= pack.items.size
          %td= pack.amount
          %td= link_to 'Show', lesson_pack_path(pack)
