.row
  %h2 Mes cours
.row
  - lessons.each do |lesson|
    -unless lesson.nil?
      .col-sm-4.preview-lesson
        .lesson-topic #{lesson.topic.title}
        .lesson-partner #{lesson.other(current_user).firstname}
        .lesson-date #{lesson.time_start.strftime("%d %b %Y")}
        .lesson-seemore #{link_to 'voir le cours', lesson_path(lesson), class: 'text-white'}
    - else
      .col-sm-4.empty-lesson.text-center
        .border
          = image_tag('dashboard/camera.png')