.row
  .col-xs-12
    %h2.text-center
      Quelques profs qui pourraient vous intéresser
    #search-results
      -teachers.each do |teacher|
        = render partial: 'users/search/search_result', :locals => {:u => teacher, :custom=>nil}